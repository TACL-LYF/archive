<!DOCTYPE html>
<html>
  <head>
    <title>LYF Camp <%= @camp.year %> Registration</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'register', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag 'favicon.ico' %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <body>
    <div class="container <%= @current_step %>">
      <% flash.each do |message_type, message| %>
        <% unless message_type == 'form_has_errors' %>
          <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
        <% end %>
      <% end %>
      <%= yield %>
      <%= render 'registration_policy' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
