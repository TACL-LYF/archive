<!DOCTYPE html>
<html>
  <head>
    <title>Donate to LYF &laquo; TACL-LYF</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'donations', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'https://js.stripe.com/v1/', 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_tag "Stripe.setPublishableKey(\'#{STRIPE_PUBLIC_KEY}\');" %>
    <%= favicon_link_tag 'favicon.ico' %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <% unless message_type == 'form_has_errors' %>
          <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
        <% end %>
      <% end %>
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          <div class="row">
            <div class="col-md-12">
              <h1><%= link_to image_tag("logo.png", size: "100", alt: "TACL-LYF Logo", class: "logo"), "https://lyf.tacl.org", target: "_blank" %> Thank you for your support!</h1>
            </div>
          </div>
          <%= yield %>
        </div>
      </div>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
