<!DOCTYPE html>
<html>
  <head>
    <title>Last Day Purchases &laquo; TACL-LYF</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'last_day_purchases', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "#{STRIPE_JS_HOST}/v1/", 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_tag "Stripe.setPublishableKey(\'#{STRIPE_PUBLIC_KEY}\');" %>
    <%= favicon_link_tag 'favicon.ico' %>
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
      </script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <% unless message_type == 'form_has_errors' %>
          <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
        <% end %>
      <% end %>
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          <div class="row">
            <div class="col-md-12">
              <%= link_to image_tag("logo.png", size: "100", alt: "TACL-LYF Logo", class: "logo pull-left"), "https://lyf.tacl.org", target: "_blank" %>
              <h1>2018 Pre-registration,<br>35th Summer Reunion,<br>Dollar for Dollar Matching</h1>
            </div>
          </div>
          <%= yield %>
        </div>
      </div>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
